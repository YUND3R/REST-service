apiVersion: v1
kind: Service
metadata:
  name: ml-balancer
  labels:
    app: ml-balancer
spec:
  type: ClusterIP
  selector:
    app: ml-balancer
  ports:
    - name: http
      port: 80
      targetPort: 80
