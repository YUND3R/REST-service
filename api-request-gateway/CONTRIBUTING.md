1. Установите зависимости через **uv**: `uv sync --all-groups`.
2. Перед PR запускайте `uv run ruff check ...` и `uv run pytest`.
3. Не коммитьте секреты; **`uv.lock`** коммитьте вместе с изменениями зависимостей.
