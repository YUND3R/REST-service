apiVersion: v1
kind: Service
metadata:
  name: ml-balancer
spec:
  type: ExternalName
  externalName: ml-balancer.cluster-b.example.invalid
  ports:
    - name: http
      port: 80
      targetPort: 80
